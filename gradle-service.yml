kind: Service
apiVersion: v1
metadata:
  name: gradle-service
spec:
  type: LoadBalancer
  selector:
    app: springboot
  ports:
    - name: http
      protocol: TCP
      port: 33333
      targetPort: 33333
